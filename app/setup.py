from setuptools import setup, find_packages
from codecs import open
from os import path

here = path.abspath( path.dirname( __file__ ) )

setup(
    name='excel-pdf-formatter',
    version='1.0.0',
    description='Python Flask app for creating Excel or PDF Files',
    license='Apache-2.0'
)
